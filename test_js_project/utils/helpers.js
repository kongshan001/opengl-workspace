// Utility functions
export const utils = {
    helper: function() {
        return "helper function";
    },
    calculator: {
        add: (a, b) => a + b,
        subtract: (a, b) => a - b
    }
};