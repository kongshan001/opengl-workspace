# Session: 2026-02-27 23:11:10 UTC

- **Session Key**: agent:opengl:main
- **Session ID**: e6b9be83-4975-4d80-9d33-67223795baf5
- **Source**: feishu

## Conversation Summary

assistant: 🔺 已完成！Socket 通信自动化测试框架已推送到 GitHub。

## 仓库地址
https://github.com/kongshan001/opencode-demo/tree/main/game-engine-starter

---

## 完成的方案

### 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    E2E 测试架构                          │
├─────────────────────────────────────────────────────────┤
│  ┌──────────────┐         Socket         ┌────────────┐ │
│  │              │ ◄──────────────────────►│            │ │
│  │  测试框架    │    JSON 指令/响应       │  游戏进程  │ │
│  │  (pytest)    │                         │ (被测对象) │ │
│  └──────────────┘                         └────────────┘ │
│        │                                        │        │
│        ▼                                        ▼        │
│  ┌──────────────┐                         ┌────────────┐ │
│  │  断言/报告   │                         │ Socket服务 │ │
│  └──────────────┘                         └────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 已创建的文件

| 文件 | 内容 |
|------|------|
| `skills/socket-testing/SKILL.md` | **32KB 完整实现文档** |
| `agents/e2e-tester.md` | E2E 测试代理 |
| `commands/e2e.md` | `/e2e` 命令 |
| `commands/generate-socket-test.md` | `/generate-socket-test` 命令 |
| `src/engine/testing/templates.py` | 代码模板 |

### 包含的完整代码

1. **GameTestServer** - 游戏内嵌 Socket 服务器
2. **GameTestClient** - 测试客户端
3. **GameLauncher** - 游戏启动器
4. **Pytest Fixtures** - 测试配置
5. **测试用例示例** - 基础测试、逻辑测试、性能测试

---

## 使用方法

### 1. 游戏端集成

```python
# main.py
from engine.testing.socket_server import GameTestServer
import argparse

parser = argparse.ArgumentParser()
parser.add_argument("--test-mode", action="store_true")
parser.add_argument("--test-port", type=int, default=9876)
args = parser.parse_args()

if args.test_mode:
    game.test_server = GameTestServer(game, port=args.test_port)
    game.test_server.start()
```

### 2. 编写测试

```python
# tests/e2e/test_game.py
def test_player_movement(paused_game):
    client = paused_game
    
    # 创建实体
    result = client.entity_create("Player", components=[...])
    player_id = result.result["entity_id"]
    
    # 执行一帧
    client.game_step(dt=1.0)
    
    # 验证结果
    transform = client.component_get(player_id, "Transform")
    assert transform.result["x"] == 10
```

### 3. 运行测试

```bash
pytest tests/e2e -v
```

---

## 标准指令协议

| 指令 | 功能 |
|------|------|
| `entity.create` | 创建实体 |
| `entity.get` | 获取实体 |
| `component.get/set` | 组件操作 |
| `game.pause/resume/step` | 游戏控制 |
| `state.get/set` | 状态操作 |
| `input.key` | 模拟输入 |
| `perf.fps` | 获取 FPS |
| `render.count` | 渲染统计 |

---

详细文档在 `.opencode/skills/socket-testing/SKILL.md` 中，包含完整的代码实现！
user: System: [2026-02-27 15:44:43 GMT+8] Feishu[opengl] DM from ou_15ff1d12b1e893ad7ae94f8124798591: E2E是什么概念，有哪些功能特性

Conversation info (untrusted metadata):
```json
{
  "message_id": "om_x100b5520f861cc84b49e979566cc7d7",
  "sender": "ou_15ff1d12b1e893ad7ae94f8124798591"
}
```

E2E是什么概念，有哪些功能特性
assistant: ## E2E (End-to-End) 端到端测试

### 概念

**E2E = End-to-End**，即"端到端"测试。

```
┌─────────────────────────────────────────────────────────────┐
│                      测试金字塔                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                    ▲ E2E 测试                               │
│                   /  \  (少量)                              │
│                  /────\  - 完整系统流程                      │
│                 /      \  - 真实环境                        │
│                /────────\                                   │
│               / 集成测试 \  (中等)                           │
│              /────────────\  - 模块交互                      │
│             /              \                                │
│            /──── 单元测试 ────\  (大量)                      │
│           /────────────────────\  - 单个函数/类              │
│          /──────────────────────\  - 隔离测试               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 核心特点

| 特点 | 说明 |
|------|------|
| **真实环境** | 测试完整的系统，不是隔离的单元 |
| **用户视角** | 模拟真实用户的操作流程 |
| **全链路** | 从输入到输出，覆盖整个流程 |
| **黑盒测试** | 不关心内部实现，只验证结果 |

---

### E2E vs 单元测试 对比

```python
# ❌ 单元测试 - 隔离测试单个函数
def test_vector_addition():
    v1 = Vector2(1, 2)
    v2 = Vector2(3, 4)
    assert (v1 + v2) == Vector2(4, 6)  # 只测试数学运算

# ✅ E2E 测试 - 测试完整游戏流程
def test_player_shoots_enemy(game_client):
    # 1. 创建玩家
    player = game_client.entity_create("Player")
    
    # 2. 创建敌人
    enemy = game_client.entity_create("Enemy")
    
    # 3. 模拟玩家射击
    game_client.input_key("SPACE")
    game_client.game_step(dt=0.1)
    
    # 4. 验证敌人被消灭
    enemy_state = game_client.entity_get(enemy.result["entity_id"])
    assert enemy_state.result["active"] == False
```

---

### E2E 测试功能特性

#### 1. 真实场景模拟

```python
def test_complete_game_level(game_client):
    """测试完整关卡流程"""
    # 加载关卡
    game_client.send_command("level.load", {"name": "level_1"})
    
    # 玩家移动
    game_client.input_key("RIGHT")
    for _ in range(60):  # 1秒
        game_client.game_step(dt=0.016)
    
    # 玩家跳跃
    game_client.input_key("SPACE")
    game_client.game_step(dt=0.5)
    
    # 验证到达终点
    result = game_client.state_get("level_completed")
    assert result.result["value"] == True
```

#### 2. 多系统交互测试

```python
def test_physics_and_collision(game_client):
    """测试物理和碰撞系统交互"""
    # 创建受重力影响的物体
    ball = game_client.entity_create(
        components=[
            {"type": "Transform", "params": {"x": 0, "y": 100}},
            {"type": "Physics", "params": {"gravity": True}},
            {"type": "Collider", "params": {"radius": 10}},
        ]
    )
    
    # 创建地面
    ground = game_client.entity_create(
        components=[
            {"type": "Transform", "params": {"x": 0, "y": 0}},
            {"type": "Collider", "params": {"width": 200, "height": 10}},
        ]
    )
    
    # 模拟 2 秒
    for _ in range(120):
        game_client.game_step(dt=0.016)
    
    # 验证球落在地面上
    transform = game_client.component_get(
        ball.result["entity_id"], "Transform"
    )
    assert transform.result["y"] < 20  # 接近地面
```

#### 3. 性能测试

```python
def test_fps_under_load(game_client):
    """测试负载下的帧率"""
    # 创建 1000 个实体
    for _ in range(1000):
        game_client.entity_create("Enemy")
    
    # 运行 100 帧
    for _ in range(100):
        game_client.game_step(dt=0.016)
    
    # 验证 FPS
    fps = game_client.perf_fps()
    assert fps.result["fps"] >= 30  # 至少 30 FPS
```

#### 4. 回归测试

```python
def test_save_load_game(game_client):
    """测试存档/读档功能"""
    # 设置游戏状态
    game_client.state_set("score", 1000)
    game_client.state_set("level", 5)
    
    # 保存
    game_client.send_command("game.save")
    
    # 修改状态
    game_client.state_set("score", 0)
    game_client.state_set("level", 1)
    
    # 加载
    game_client.send_command("game.load")
    
    # 验证恢复
    score = game_client.state_get("score")
    level = game_client.state_get("level")
    
    assert score.result["value"] == 1000
    assert level.result["value"] == 5
```

---

### 游戏开发中 E2E 测试的价值

| 场景 | E2E 测试作用 |
|------|-------------|
| **关卡流程** | 验证玩家能完成整个关卡 |
| **存档系统** | 确保存档/读档正确 |
| **UI 交互** | 测试菜单、按钮响应 |
| **多人游戏** | 测试网络同步 |
| **性能压测** | 大量实体下的稳定性 |
| **输入系统** | 键盘/鼠标/手柄响应 |
| **音效触发** | 特定事件播放正确音效 |

---

### 你的 Socket E2E 测试方案

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  测试脚本                        游戏进程                    │
│  (pytest)                       (被测对象)                  │
│                                                             │
│  ┌─────────────┐         Socket          ┌─────────────┐   │
│  │ 1. 启动游戏 │ ───────────────────────►│             │   │
│  │ 2. 发送指令 │     JSON 协议           │  执行指令   │   │
│  │ 3. 接收结果 │ ◄───────────────────────│  返回结果   │   │
│  │ 4. 断言验证 │                         │             │   │
│  └─────────────┘                         └─────────────┘   │
│                                                             │
│  指令示例:                                                  │
│  • entity.create   - 创建实体                              │
│  • game.step       - 执行一帧                              │
│  • input.key       - 模拟按键                              │
│  • state.get       - 获取状态                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 简单示例

```python
def test_player_can_move(game_client):
    """E2E: 玩家可以移动"""
    
    # 1. 创建玩家（起点）
    player = game_client.entity_create(
        "Player",
        components=[{"type": "Transform", "params": {"x": 0, "y": 0}}]
    )
    player_id = player.result["entity_id"]
    
    # 2. 模拟按下右键
    game_client.input_key("RIGHT", action="press")
    
    # 3. 执行 60 帧（1秒）
    for _ in range(60):
        game_client.game_step(dt=0.016)
    
    # 4. 释放按键
    game_client.input_key("RIGHT", action="release")
    
    # 5. 验证玩家位置变化（终点）
    transform = game_client.component_get(player_id, "Transform")
    assert transform.result["x"] > 0  # 玩家向右移动了
    
    # ✅ 端到端验证完成！
```

这就是 **从输入（按键）到输出（位置变化）的完整链路测试**，即 E2E 测试。
user: System: [2026-02-27 15:47:39 GMT+8] Feishu[opengl] DM from ou_15ff1d12b1e893ad7ae94f8124798591: 支持启动游戏客户端跑测试用例吗

Conversation info (untrusted metadata):
```json
{
  "message_id": "om_x100b55208f5174a4b3986951fa9b4b8",
  "sender": "ou_15ff1d12b1e893ad7ae94f8124798591"
}
```

支持启动游戏客户端跑测试用例吗
assistant: **完全支持！** 这正是 Socket E2E 测试框架的核心功能。

## 工作流程

```
┌──────────────────────────────────────────────────────────────────┐
│                      自动化测试流程                               │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   pytest                                                          │
│     │                                                             │
│     ▼                                                             │
│   ┌─────────────┐      subprocess.Popen      ┌─────────────┐     │
│   │ GameLauncher│ ─────────────────────────► │  main.py    │     │
│   │             │      启动游戏进程           │ --test-mode │     │
│   └─────────────┘                            └─────────────┘     │
│         │                                           │            │
│         │ 等待 Socket 就绪                          │            │
│         ▼                                           ▼            │
│   ┌─────────────┐      Socket 连接           ┌─────────────┐     │
│   │GameTestClient│ ◄────────────────────────►│GameTestServer│    │
│   │             │      JSON 指令/响应        │             │     │
│   └─────────────┘                            └─────────────┘     │
│         │                                                             │
│         ▼                                                             │
│   执行测试用例                                                     │
│         │                                                             │
│         ▼                                                             │
│   断言验证                                                         │
│         │                                                             │
│         ▼                                                             │
│   关闭游戏进程                                                   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

## 核心代码

### 1. 游戏启动器

```python
# tests/e2e/launcher.py
import subprocess
import socket
import time

class GameLauncher:
    """启动游戏进程并管理生命周期"""
    
    def __init__(self, game_path: str, port: int = 9876):
        self.game_path = game_path
        self.port = port
        self.process = None
    
    def start(self):
        """启动游戏"""
        # 启动游戏进程（测试模式）
        self.process = subprocess.Popen([
            "python", self.game_path,
            "--test-mode",
            "--test-port", str(self.port)
        ])
        
        # 等待 Socket 就绪
        self._wait_for_ready()
        return self.process
    
    def _wait_for_ready(self, timeout: float = 10.0):
        """等待游戏 Socket 服务就绪"""
        start = time.perf_counter()
        while time.perf_counter() - start < timeout:
            try:
                sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
                sock.settimeout(0.5)
                sock.connect(("localhost", self.port))
                sock.close()
                return  # 连接成功
            except:
                time.sleep(0.1)
        raise TimeoutError("游戏启动超时")
    
    def stop(self):
        """关闭游戏"""
        if self.process:
            self.process.terminate()
            self.process.wait()
```

### 2. Pytest Fixture 配置

```python
# tests/e2e/conftest.py
import pytest
from tests.e2e.launcher import GameLauncher
from tests.e2e.client import GameTestClient

@pytest.fixture(scope="function")
def game_client():
    """每个测试自动启动游戏、连接、测试、关闭"""
    
    # 1. 启动游戏
    launcher = GameLauncher("main.py", port=9876)
    launcher.start()
    
    # 2. 连接游戏
    client = GameTestClient(port=9876)
    client.connect()
    
    try:
        # 3. 执行测试（yield 给测试用例）
        yield client
    finally:
        # 4. 断开连接
        client.disconnect()
        
        # 5. 关闭游戏
        launcher.stop()
```

### 3. 测试用例

```python
# tests/e2e/test_player.py

def test_player_can_move(game_client):
    """测试：玩家可以移动"""
    
    # 游戏已自动启动，client 已连接
    
    # 创建玩家
    player = game_client.entity_create("Player")
    player_id = player.result["entity_id"]
    
    # 模拟输入
    game_client.input_key("RIGHT", action="tap")
    game_client.game_step(dt=0.1)
    
    # 验证
    transform = game_client.component_get(player_id, "Transform")
    assert transform.result["x"] > 0
    
    # 测试结束，游戏自动关闭
```

## 运行测试

```bash
# 运行单个测试
pytest tests/e2e/test_player.py -v

# 运行所有 E2E 测试
pytest tests/e2e -v

# 带日志输出
pytest tests/e2e -v -s
```

## 输出示例

```
$ pytest tests/e2e/test_player.py -v

================ test session starts ================
tests/e2e/test_player.py::test_player_can_move
  [启动游戏] python main.py --test-mode --test-port 9876
  [等待就绪] 连接 Socket...
  [已连接] localhost:9876
  [执行测试] test_player_can_move
  [发送指令] entity.create
  [接收响应] {"success": true, "entity_id": 1}
  [发送指令] input.key
  [接收响应] {"success": true}
  [发送指令] game.step
  [接收响应] {"success": true}
  [发送指令] component.get
  [接收响应] {"success": true, "result": {"x": 10}}
  [断言通过] x > 0
  [关闭游戏] terminate process
PASSED

================ 1 passed in 2.34s ================
```

## 总结

| 功能 | 支持 |
|------|------|
| 自动启动游戏 | ✅ |
| 等待就绪 | ✅ |
| Socket 通信 | ✅ |
| 发送指令 | ✅ |
| 接收结果 | ✅ |
| 断言验证 | ✅ |
| 自动关闭游戏 | ✅ |

**这就是完整的"启动游戏客户端 → 跑测试用例 → 自动关闭"的自动化流程！**
user: System: [2026-02-27 15:50:48 GMT+8] Feishu[opengl] DM from ou_15ff1d12b1e893ad7ae94f8124798591: 还是不太清楚具体怎么对接，我目前启动游戏是通过exe文件

Conversation info (untrusted metadata):
```json
{
  "message_id": "om_x100b552083b5aca4c26c2bb9090cbe5",
  "sender": "ou_15ff1d12b1e893ad7ae94f8124798591"
}
```

还是不太清楚具体怎么对接，我目前启动游戏是通过exe文件
assistant: 明白了！你用的是 **exe 文件**启动游戏。我来给你一个清晰的对接步骤。

## 对接步骤

### 第一步：游戏端集成 Socket 服务器

在你的游戏代码中添加测试服务器：

```python
# 在你的游戏入口文件（如 main.py）添加

from engine.testing.socket_server import GameTestServer

class Game:
    def __init__(self):
        # ... 你的初始化代码 ...
        
        # 添加：测试服务器
        self.test_server = None
    
    def run(self):
        # 添加：检查是否是测试模式
        import sys
        if "--test-mode" in sys.argv:
            port = 9876
            for arg in sys.argv:
                if arg.startswith("--test-port="):
                    port = int(arg.split("=")[1])
            
            self.test_server = GameTestServer(self, port=port)
            self.test_server.start()
        
        try:
            # 你的游戏主循环
            while self.is_running:
                self.update()
                self.render()
        finally:
            if self.test_server:
                self.test_server.stop()

# 启动
if __name__ == "__main__":
    game = Game()
    game.run()
```

### 第二步：重新打包 exe

```bash
# 使用 PyInstaller 打包
pyinstaller --onefile main.py --name mygame

# 生成的 exe 支持：
# mygame.exe --test-mode --test-port=9876
```

### 第三步：修改启动器

```python
# tests/e2e/launcher.py

class GameLauncher:
    def __init__(self, exe_path: str, port: int = 9876):
        self.exe_path = exe_path  # "D:/games/mygame.exe"
        self.port = port
        self.process = None
    
    def start(self):
        import subprocess
        
        # 启动 exe（测试模式）
        self.process = subprocess.Popen([
            self.exe_path,
            "--test-mode",
            f"--test-port={self.port}"
        ])
        
        # 等待 Socket 就绪
        self._wait_for_ready()
        return self
```

### 第四步：配置测试

```python
# tests/e2e/conftest.py

import pytest
from tests.e2e.launcher import GameLauncher
from tests.e2e.client import GameTestClient

# 你的 exe 路径
EXE_PATH = r"D:\mygame\mygame.exe"  # 改成你的路径

@pytest.fixture(scope="function")
def game_client():
    """自动启动 exe、测试、关闭"""
    
    # 1. 启动 exe
    launcher = GameLauncher(EXE_PATH, port=9876)
    launcher.start()
    
    # 2. 连接
    client = GameTestClient(port=9876)
    client.connect()
    
    try:
        yield client
    finally:
        # 3. 关闭
        client.disconnect()
        launcher.stop()
```

### 第五步：写测试

```python
# tests/e2e/test_basic.py

def test_game_starts(game_client):
    """测试：游戏能启动"""
    result = game_client.state_get()
    assert result.success

def test_player_exists(game_client):
    """测试：玩家实体存在"""
    result = game_client.entity_create("Player")
    assert result.success
```

### 第六步：运行测试

```bash
# 会自动：启动 exe → 测试 → 关闭 exe
pytest tests/e2e -v
```

---

## 如果 exe 不支持命令行参数

如果你的 exe 不能传参数，可以用 **配置文件** 方式：

### 方案 A：配置文件

```python
# 游戏启动时检查
import os

class Game:
    def __init__(self):
        # 检查测试模式配置文件
        if os.path.exists("test_mode.json"):
            import json
            with open("test_mode.json") as f:
                config = json.load(f)
            
            self.test_server = GameTestServer(
                self, 
                port=config.get("port", 9876)
            )
            self.test_server.start()
```

```python
# tests/e2e/launcher.py

class GameLauncher:
    def start(self):
        import json
        
        # 1. 写入配置文件
        with open("test_mode.json", "w") as f:
            json.dump({"port": self.port}, f)
        
        # 2. 启动 exe
        self.process = subprocess.Popen([self.exe_path])
        
        # 3. 等待就绪
        self._wait_for_ready()
    
    def stop(self):
        # 删除配置文件
        import os
        if os.path.exists("test_mode.json"):
            os.remove("test_mode.json")
        
        # 关闭进程
        self.process.terminate()
```

### 方案 B：环境变量

```python
# 游戏启动时检查
import os

class Game:
    def __init__(self):
        # 检查环境变量
        if os.environ.get("GAME_TEST_MODE") == "1":
            port = int(os.environ.get("GAME_TEST_PORT", "9876"))
            self.test_server = GameTestServer(self, port=port)
            self.test_server.start()
```

```python
# tests/e2e/launcher.py

class GameLauncher:
    def start(self):
        import os
        
        # 设置环境变量
        os.environ["GAME_TEST_MODE"] = "1"
        os.environ["GAME_TEST_PORT"] = str(self.port)
        
        # 启动 exe（会继承环境变量）
        self.process = subprocess.Popen([self.exe_path])
        
        self._wait_for_ready()
```

---

## 总结

```
┌────────────────────────────────────────────────────────┐
│                    完整流程                            │
├────────────────────────────────────────────────────────┤
│                                                        │
│  1. 在游戏代码中集成 GameTestServer                    │
│  2. 打包 exe                                           │
│  3. pytest 启动 GameLauncher                           │
│     └─► subprocess.Popen(["mygame.exe", ...])         │
│  4. exe 启动，Socket 服务器监听                        │
│  5. GameTestClient 连接                                │
│  6. 执行测试用例                                       │
│  7. 自动关闭 exe                                       │
│                                                        │
└────────────────────────────────────────────────────────┘
```

**关键点：你的游戏代码需要集成 `GameTestServer`，然后重新打包 exe。**

需要我帮你写具体的集成代码吗？告诉我你的游戏入口文件结构。
user: System: [2026-02-27 15:52:39 GMT+8] Feishu[opengl] DM from ou_15ff1d12b1e893ad7ae94f8124798591: 你通过配置文件的方式给我配置吧，另外上述的信息也整理到文档中

Conversation info (untrusted metadata):
```json
{
  "message_id": "om_x100b55209abd20acc2cdf0bb2e05de3",
  "sender": "ou_15ff1d12b1e893ad7ae94f8124798591"
}
```

你通过配置文件的方式给我配置吧，另外上述的信息也整理到文档中
assistant: 好的，我来创建完整的配置文件方案并更新文档。
assistant: 🔺 完成！已创建完整的 **配置文件方式** Socket E2E 测试框架。

## 仓库地址
https://github.com/kongshan001/opencode-demo/tree/main/game-engine-starter

---

## 📦 创建的文件

### 游戏端（集成到你的游戏）

| 文件 | 功能 |
|------|------|
| `src/engine/testing/socket_server.py` | Socket 服务器（接收测试指令） |
| `src/engine/testing/config.py` | 配置文件读取器 |
| `src/engine/testing/integration.py` | 集成示例代码 |

### 测试端（运行测试）

| 文件 | 功能 |
|------|------|
| `tests/e2e/launcher.py` | 游戏启动器（写配置→启动 exe→等待就绪） |
| `tests/e2e/client.py` | 测试客户端（发送指令） |
| `tests/e2e/conftest.py` | Pytest 配置 |
| `tests/e2e/test_basic.py` | 基础测试示例 |
| `tests/e2e/test_game_logic.py` | 逻辑测试示例 |
| `tests/e2e/README.md` | 使用说明 |

---

## 🔧 使用步骤

### 第一步：游戏端集成

将以下代码添加到你的 `main.py`：

```python
from engine.testing.socket_server import GameTestServer
from engine.testing.config import TestModeConfig

# 创建游戏
game = MyGame()

# 检查测试模式（读取 test_mode.json）
config = TestModeConfig.from_file()

# 启动测试服务器
test_server = None
if config.enabled:
    test_server = GameTestServer(game, port=config.port)
    test_server.start()

try:
    game.run()
finally:
    if test_server:
        test_server.stop()
```

### 第二步：打包 exe

```bash
pyinstaller --onefile --name mygame main.py
```

### 第三步：配置测试

修改 `tests/e2e/conftest.py`：

```python
# 改成你的 exe 路径
EXE_PATH = r"D:\mygame\mygame.exe"
```

### 第四步：运行测试

```bash
pytest tests/e2e -v
```

---

## 📋 工作流程

```
pytest 运行
    ↓
GameLauncher 写入 test_mode.json
    ↓
启动 exe（exe 读取配置，启动 Socket 服务器）
    ↓
GameTestClient 连接 Socket
    ↓
发送测试指令 → 游戏执行 → 返回结果
    ↓
断言验证
    ↓
自动关闭 exe，删除配置文件
```

---

## 📝 配置文件格式

`test_mode.json`（自动生成在 exe 同目录）：

```json
{
  "enabled": true,
  "port": 9876,
  "host": "localhost",
  "auto_start": true,
  "timeout": 30.0
}
```

---

完整文档在 `.opencode/skills/socket-testing/SKILL.md`（33KB）！
user: System: [2026-02-27 16:16:24 GMT+8] Feishu[opengl] DM from ou_15ff1d12b1e893ad7ae94f8124798591: 那游戏客户端是如何对接，例如如何通知游戏点击道具图标

Conversation info (untrusted metadata):
```json
{
  "message_id": "om_x100b552163b31cb4b328fb102aa548c",
  "sender": "ou_15ff1d12b1e893ad7ae94f8124798591"
}
```

那游戏客户端是如何对接，例如如何通知游戏点击道具图标
assistant: 好问题！我来详细说明游戏端如何对接 UI 交互测试。

## UI 交互测试对接

### 架构

```
┌──────────────────────────────────────────────────────────────────┐
│                    UI 交互测试流程                                │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   测试脚本                                                        │
│     │                                                             │
│     │  ui.click(element="道具图标")                               │
│     │  ui.click(element="btn_ok")                                 │
│     │  ui.click(x=100, y=200)                                    │
│     ▼                                                             │
│   GameTestClient                                                  │
│     │                                                             │
│     │  Socket JSON 指令                                          │
│     ▼                                                             │
│   GameTestServer ───► UI系统 ───► 执行点击                        │
│     │                           │                                 │
│     │                           ▼                                 │
│     │                     触发点击事件                             │
│     │                           │                                 │
│     │                           ▼                                 │
│     │                     道具被选中                              │
│     │                           │                                 │
│     ◄───────────────────────────┘                                │
│     返回结果                                                       │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 一、游戏端 UI 系统对接

### 1.1 UI 元素定位方式

```python
# src/engine/ui/element.py
"""
UI 元素基类
"""

class UIElement:
    """UI 元素基类"""
    
    def __init__(self, element_id: str, name: str = ""):
        self.element_id = element_id  # 唯一 ID
        self.name = name              # 可读名称
        self.x = 0
        self.y = 0
        self.width = 0
        self.height = 0
        self.visible = True
        self.enabled = True
        self.parent = None
        self.children = []
    
    def get_bounds(self):
        """获取边界"""
        return (self.x, self.y, self.x + self.width, self.y + self.height)
    
    def contains_point(self, x: int, y: int) -> bool:
        """检查点是否在元素内"""
        return (self.x <= x < self.x + self.width and
                self.y <= y < self.y + self.height)
    
    def click(self):
        """点击事件"""
        pass
    
    def to_dict(self):
        """序列化"""
        return {
            "id": self.element_id,
            "name": self.name,
            "x": self.x,
            "y": self.y,
            "width": self.width,
            "height": self.height,
            "visible": self.visible,
            "enabled": self.enabled,
        }
```

### 1.2 UI 管理器

```python
# src/engine/ui/manager.py
"""
UI 管理器 - 管理所有 UI 元素
"""

from typing import Dict, List, Optional, Any
from .element import UIElement


class UIManager:
    """UI 管理器"""
    
    def __init__(self):
        self._elements: Dict[str, UIElement] = {}
        self._root_elements: List[UIElement] = []
        self._focused_element: Optional[UIElement] = None
    
    def register(self, element: UIElement):
        """注册 UI 元素"""
        self._elements[element.element_id] = element
        if element.parent is None:
            self._root_elements.append(element)
    
    def unregister(self, element: UIElement):
        """注销 UI 元素"""
        if element.element_id in self._elements:
            del self._elements[element.element_id]
        if element in self._root_elements:
            self._root_elements.remove(element)
    
    def get_element(self, element_id: str) -> Optional[UIElement]:
        """通过 ID 获取元素"""
        return self._elements.get(element_id)
    
    def find_element_by_name(self, name: str) -> Optional[UIElement]:
        """通过名称查找元素"""
        for element in self._elements.values():
            if element.name == name:
                return element
        return None
    
    def find_element_by_path(self, path: str) -> Optional[UIElement]:
        """通过路径查找元素
        
        例如: "inventory_panel/item_slot_1"
        """
        parts = path.split("/")
        current = None
        
        for part in parts:
            if current is None:
                # 查找根元素
                for elem in self._root_elements:
                    if elem.element_id == part or elem.name == part:
                        current = elem
                        break
            else:
                # 查找子元素
                for child in current.children:
                    if child.element_id == part or child.name == part:
                        current = child
                        break
        
        return current
    
    def get_element_at(self, x: int, y: int) -> Optional[UIElement]:
        """获取指定坐标的元素"""
        # 从后往前遍历（后添加的在上面）
        for element in reversed(list(self._elements.values())):
            if element.visible and element.contains_point(x, y):
                return element
        return None
    
    def click_at(self, x: int, y: int) -> bool:
        """点击指定坐标"""
        element = self.get_element_at(x, y)
        if element and element.enabled:
            element.click()
            return True
        return False
    
    def click_element(self, element_id: str = None, name: str = None, path: str = None) -> bool:
        """点击指定元素
        
        Args:
            element_id: 元素 ID
            name: 元素名称
            path: 元素路径
        """
        element = None
        
        if element_id:
            element = self.get_element(element_id)
        elif name:
            element = self.find_element_by_name(name)
        elif path:
            element = self.find_element_by_path(path)
        
        if element and element.visible and element.enabled:
            element.click()
            return True
        
        return False
    
    def list_elements(self, visible_only: bool = True) -> List[dict]:
        """列出所有元素"""
        elements = []
        for element in self._elements.values():
            if not visible_only or element.visible:
                elements.append(element.to_dict())
        return elements
    
    def get_hierarchy(self) -> dict:
        """获取 UI 层级结构"""
        def build_tree(element: UIElement) -> dict:
            return {
                **element.to_dict(),
                "children": [build_tree(child) for child in element.children]
            }
        
        return {
            "root_elements": [build_tree(e) for e in self._root_elements]
        }
```

### 1.3 UI 元素示例

```python
# src/engine/ui/widgets.py
"""
UI 组件示例
"""

from .element import UIElement
from typing import Callable, Optional


class Button(UIElement):
    """按钮"""
    
    def __init__(self, element_id: str, name: str = "", on_click: Callable = None):
        super().__init__(element_id, name)
        self.on_click = on_click
        self.text = ""
    
    def click(self):
        """点击按钮"""
        if self.enabled and self.on_click:
            self.on_click()
    
    def to_dict(self):
        return {
            **super().to_dict(),
            "type": "Button",
            "text": self.text,
        }


class ItemSlot(UIElement):
    """道具槽"""
    
    def __init__(self, element_id: str, slot_index: int = 0):
        super().__init__(element_id, f"道具槽_{slot_index}")
        self.slot_index = slot_index
        self.item: Optional[dict] = None  # 道具数据
    
    def click(self):
        """点击道具槽"""
        if self.enabled:
            # 触发道具选择事件
            print(f"[UI] 点击道具槽 {self.slot_index}: {self.item}")
    
    def set_item(self, item: dict):
        """设置道具"""
        self.item = item
    
    def to_dict(self):
        return {
            **super().to_dict(),
            "type": "ItemSlot",
            "slot_index": self.slot_index,
            "item": self.item,
        }


class InventoryPanel(UIElement):
    """背包面板"""
    
    def __init__(self, element_id: str = "inventory_panel"):
        super().__init__(element_id, "背包")
        self.x = 100
        self.y = 100
        self.width = 400
        self.height = 300
        
        # 创建道具槽
        self.slots = []
        for i in range(20):  # 20 个格子
            slot = ItemSlot(f"item_slot_{i}", i)
            slot.x = 10 + (i % 5) * 76
            slot.y = 40 + (i // 5) * 76
            slot.width = 70
            slot.height = 70
            slot.parent = self
            self.children.append(slot)
            self.slots.append(slot)
    
    def get_slot(self, index: int) -> Optional[ItemSlot]:
        """获取指定索引的道具槽"""
        if 0 <= index < len(self.slots):
            return self.slots[index]
        return None
    
    def to_dict(self):
        return {
            **super().to_dict(),
            "type": "InventoryPanel",
        }
```

---

## 二、扩展 Socket 服务器支持 UI

### 2.1 添加 UI 指令处理器

```python
# src/engine/testing/socket_server.py
# 在 GameTestServer 类中添加

class GameTestServer:
    # ... 原有代码 ...
    
    def __init__(self, game, host: str = "localhost", port: int = 9876):
        # ... 原有初始化 ...
        
        # 添加 UI 指令处理器
        self._handlers.update({
            # UI 操作
            "ui.click": self._handle_ui_click,
            "ui.click_at": self._handle_ui_click_at,
            "ui.get_element": self._handle_ui_get_element,
            "ui.list_elements": self._handle_ui_list_elements,
            "ui.get_hierarchy": self._handle_ui_get_hierarchy,
            "ui.wait_for_element": self._handle_ui_wait_for_element,
            
            # 道具相关
            "item.use": self._handle_item_use,
            "item.equip": self._handle_item_equip,
            "item.drop": self._handle_item_drop,
            "item.get_info": self._handle_item_get_info,
        })
    
    # ============================================================
    # UI 指令处理器
    # ============================================================
    
    def _handle_ui_click(self, params: dict) -> dict:
        """点击 UI 元素
        
        参数:
            - element_id: 元素 ID
            - name: 元素名称
            - path: 元素路径（如 "inventory_panel/item_slot_0"）
        """
        ui_manager = self._get_ui_manager()
        if not ui_manager:
            raise NotImplementedError("UI manager not available")
        
        element_id = params.get("element_id")
        name = params.get("name")
        path = params.get("path")
        
        success = ui_manager.click_element(
            element_id=element_id,
            name=name,
            path=path
        )
        
        return {
            "success": success,
            "element_id": element_id,
            "name": name,
            "path": path,
        }
    
    def _handle_ui_click_at(self, params: dict) -> dict:
        """点击指定坐标
        
        参数:
            - x: X 坐标
            - y: Y 坐标
        """
        ui_manager = self._get_ui_manager()
        if not ui_manager:
            raise NotImplementedError("UI manager not available")
        
        x = params["x"]
        y = params["y"]
        
        # 获取点击的元素
        element = ui_manager.get_element_at(x, y)
        element_info = element.to_dict() if element else None
        
        # 执行点击
        success = ui_manager.click_at(x, y)
        
        return {
            "success": success,
            "x": x,
            "y": y,
            "element": element_info,
        }
    
    def _handle_ui_get_element(self, params: dict) -> dict:
        """获取 UI 元素信息
        
        参数:
            - element_id: 元素 ID
            - name: 元素名称
            - path: 元素路径
        """
        ui_manager = self._get_ui_manager()
        if not ui_manager:
            raise NotImplementedError("UI manager not available")
        
        element_id = params.get("element_id")
        name = params.get("name")
        path = params.get("path")
        
        element = None
        if element_id:
            element = ui_manager.get_element(element_id)
        elif name:
            element = ui_manager.find_element_by_name(name)
        elif path:
            element = ui_manager.find_element_by_path(path)
        
        if element is None:
            raise ValueError("Element not found")
        
        return element.to_dict()
    
    def _handle_ui_list_elements(self, params: dict) -> dict:
        """列出所有 UI 元素"""
        ui_manager = self._get_ui_manager()
        if not ui_manager:
            raise NotImplementedError("UI manager not available")
        
        visible_only = params.get("visible_only", True)
        elements = ui_manager.list_elements(visible_only)
        
        return {
            "count": len(elements),
            "elements": elements,
        }
    
    def _handle_ui_get_hierarchy(self, params: dict) -> dict:
        """获取 UI 层级结构"""
        ui_manager = self._get_ui_manager()
        if not ui_manager:
            raise NotImplementedError("UI manager not available")
        
        return ui_manager.get_hierarchy()
    
    def _handle_ui_wait_for_element(self, params: dict) -> dict:
        """等待元素出现
        
        参数:
            - element_id: 元素 ID
            - name: 元素名称
            - timeout: 超时时间（秒）
        """
        import time
        
        ui_manager = self._get_ui_manager()
        if not ui_manager:
            raise NotImplementedError("UI manager not available")
        
        element_id = params.get("element_id")
        name = params.get("name")
        timeout = params.get("timeout", 5.0)
        
        start = time.perf_counter()
        
        while time.perf_counter() - start < timeout:
            element = None
            if element_id:
                element = ui_manager.get_element(element_id)
            elif name:
                element = ui_manager.find_element_by_name(name)
            
            if element and element.visible:
                return {
                    "found": True,
                    "element": element.to_dict(),
                }
            
            time.sleep(0.1)
        
        return {
            "found": False,
            "error": "Timeout waiting for element",
        }
    
    # ============================================================
    # 道具相关指令
    # ============================================================
    
    def _handle_item_use(self, params: dict) -> dict:
        """使用道具
        
        参数:
            - slot_index: 道具槽索引
            - item_id: 道具 ID
        """
        # 方式1：通过槽位索引
        slot_index = params.get("slot_index")
        if slot_index is not None:
            # 模拟点击道具槽
            ui_manager = self._get_ui_manager()
            if ui_manager:
                inventory = ui_manager.get_element("inventory_panel")
                if inventory and hasattr(inventory, 'get_slot'):
                    slot = inventory.get_slot(slot_index)
                    if slot:
                        slot.click()
                        return {"success": True, "slot_index": slot_index}
        
        # 方式2：通过道具 ID
        item_id = params.get("item_id")
        if item_id:
            if hasattr(self.game, 'use_item'):
                self.game.use_item(item_id)
                return {"success": True, "item_id": item_id}
        
        raise ValueError("Failed to use item")
    
    def _handle_item_equip(self, params: dict) -> dict:
        """装备道具"""
        item_id = params["item_id"]
        
        if hasattr(self.game, 'equip_item'):
            self.game.equip_item(item_id)
            return {"success": True}
        
        raise NotImplementedError("equip_item not implemented")
    
    def _handle_item_drop(self, params: dict) -> dict:
        """丢弃道具"""
        item_id = params["item_id"]
        
        if hasattr(self.game, 'drop_item'):
            self.game.drop_item(item_id)
            return {"success": True}
        
        raise NotImplementedError("drop_item not implemented")
    
    def _handle_item_get_info(self, params: dict) -> dict:
        """获取道具信息"""
        item_id = params.get("item_id")
        slot_index = params.get("slot_index")
        
        if slot_index is not None:
            ui_manager = self._get_ui_manager()
            if ui_manager:
                inventory = ui_manager.get_element("inventory_panel")
                if inventory and hasattr(inventory, 'get_slot'):
                    slot = inventory.get_slot(slot_index)
                    if slot:
                        return {
                            "slot_index": slot_index,
                            "item": slot.item,
                        }
        
        raise ValueError("Item not found")
    
    def _get_ui_manager(self):
        """获取 UI 管理器"""
        return getattr(self.game, 'ui_manager', None) or \
               getattr(self.game, 'ui', None)
```

---

## 三、测试端使用

### 3.1 扩展客户端

```python
# tests/e2e/client.py
# 在 GameTestClient 类中添加

class GameTestClient:
    # ... 原有代码 ...
    
    # ============================================================
    # UI 操作方法
    # ============================================================
    
    def ui_click(self, element_id: str = None, name: str = None, path: str = None) -> CommandResult:
        """点击 UI 元素
        
        三种定位方式，任选其一：
        - element_id: 元素 ID
        - name: 元素名称
        - path: 元素路径
        """
        params = {}
        if element_id:
            params["element_id"] = element_id
        if name:
            params["name"] = name
        if path:
            params["path"] = path
        
        return self.send_command("ui.click", params)
    
    def ui_click_at(self, x: int, y: int) -> CommandResult:
        """点击指定坐标"""
        return self.send_command("ui.click_at", {"x": x, "y": y})
    
    def ui_get_element(self, element_id: str = None, name: str = None, path: str = None) -> CommandResult:
        """获取 UI 元素信息"""
        params = {}
        if element_id:
            params["element_id"] = element_id
        if name:
            params["name"] = name
        if path:
            params["path"] = path
        
        return self.send_command("ui.get_element", params)
    
    def ui_list_elements(self, visible_only: bool = True) -> CommandResult:
        """列出所有 UI 元素"""
        return self.send_command("ui.list_elements", {"visible_only": visible_only})
    
    def ui_get_hierarchy(self) -> CommandResult:
        """获取 UI 层级结构"""
        return self.send_command("ui.get_hierarchy")
    
    def ui_wait_for_element(self, element_id: str = None, name: str = None, timeout: float = 5.0) -> CommandResult:
        """等待元素出现"""
        params = {"timeout": timeout}
        if element_id:
            params["element_id"] = element_id
        if name:
            params["name"] = name
        
        return self.send_command("ui.wait_for_element", params)
    
    # ============================================================
    # 道具操作方法
    # ============================================================
    
    def item_use(self, slot_index: int = None, item_id: str = None) -> CommandResult:
        """使用道具"""
        params = {}
        if slot_index is not None:
            params["slot_index"] = slot_index
        if item_id:
            params["item_id"] = item_id
        
        return self.send_command("item.use", params)
    
    def item_equip(self, item_id: str) -> CommandResult:
        """装备道具"""
        return self.send_command("item.equip", {"item_id": item_id})
    
    def item_drop(self, item_id: str) -> CommandResult:
        """丢弃道具"""
        return self.send_command("item.drop", {"item_id": item_id})
    
    def item_get_info(self, slot_index: int = None, item_id: str = None) -> CommandResult:
        """获取道具信息"""
        params = {}
        if slot_index is not None:
            params["slot_index"] = slot_index
        if item_id:
            params["item_id"] = item_id
        
        return self.send_command("item.get_info", params)
```

### 3.2 测试用例示例

```python
# tests/e2e/test_ui.py
"""
UI 交互测试
"""

import pytest


class TestUIInteraction:
    """UI 交互测试"""
    
    def test_list_ui_elements(self, game_client):
        """测试：列出 UI 元素"""
        result = game_client.ui_list_elements()
        
        assert result.success
        print(f"UI 元素数量: {result.result['count']}")
        for elem in result.result['elements']:
            print(f"  - {elem['id']}: {elem['name']}")
    
    def test_get_ui_hierarchy(self, game_client):
        """测试：获取 UI 层级"""
        result = game_client.ui_get_hierarchy()
        
        assert result.success
        print(f"UI 层级: {result.result}")
    
    def test_click_by_id(self, game_client):
        """测试：通过 ID 点击"""
        result = game_client.ui_click(element_id="btn_start")
        
        assert result.success
    
    def test_click_by_name(self, game_client):
        """测试：通过名称点击"""
        result = game_client.ui_click(name="开始游戏")
        
        assert result.success
    
    def test_click_by_path(self, game_client):
        """测试：通过路径点击"""
        result = game_client.ui_click(path="main_menu/btn_start")
        
        assert result.success
    
    def test_click_at_coordinates(self, game_client):
        """测试：点击坐标"""
        result = game_client.ui_click_at(x=400, y=300)
        
        assert result.success
        print(f"点击的元素: {result.result['element']}")


class TestItemInteraction:
    """道具交互测试"""
    
    def test_click_item_slot(self, game_client):
        """测试：点击道具槽"""
        # 方式1：通过路径
        result = game_client.ui_click(path="inventory_panel/item_slot_0")
        assert result.success
    
    def test_use_item_by_slot(self, game_client):
        """测试：使用道具（通过槽位）"""
        result = game_client.item_use(slot_index=0)
        
        assert result.success
    
    def test_use_item_by_id(self, game_client):
        """测试：使用道具（通过 ID）"""
        result = game_client.item_use(item_id="potion_hp_001")
        
        assert result.success
    
    def test_get_item_info(self, game_client):
        """测试：获取道具信息"""
        result = game_client.item_get_info(slot_index=0)
        
        assert result.success
        print(f"道具信息: {result.result['item']}")
    
    def test_complete_item_flow(self, paused_game):
        """测试：完整的道具使用流程"""
        client = paused_game
        
        # 1. 打开背包
        result = client.input_key("I")  # 假设 I 键打开背包
        assert result.success
        
        # 2. 等待背包面板出现
        result = client.ui_wait_for_element(name="背包", timeout=2.0)
        assert result.result["found"]
        
        # 3. 获取第一个道具信息
        result = client.item_get_info(slot_index=0)
        if result.success and result.result.get("item"):
            item = result.result["item"]
            print(f"道具: {item}")
            
            # 4. 使用道具
            result = client.item_use(slot_index=0)
            assert result.success
            
            # 5. 验证道具被使用（根据你的游戏逻辑）
            # ...


class TestComplexUIFlow:
    """复杂 UI 流程测试"""
    
    def test_shop_buy_item(self, paused_game):
        """测试：商店购买道具"""
        client = paused_game
        
        # 1. 点击 NPC 打开商店
        result = client.ui_click(name="商人NPC")
        assert result.success
        
        # 2. 等待商店面板
        result = client.ui_wait_for_element(name="商店面板", timeout=2.0)
        assert result.result["found"]
        
        # 3. 点击购买按钮
        result = client.ui_click(path="shop_panel/btn_buy")
        assert result.success
        
        # 4. 确认购买
        result = client.ui_click(name="确认")
        assert result.success
    
    def test_settings_change(self, paused_game):
        """测试：修改设置"""
        client = paused_game
        
        # 1. 打开设置
        result = client.ui_click(name="设置")
        assert result.success
        
        # 2. 修改音量滑块（点击滑块位置）
        result = client.ui_click_at(x=300, y=200)  # 滑块位置
        assert result.success
        
        # 3. 保存设置
        result = client.ui_click(name="保存")
        assert result.success
```

---

## 四、完整对接清单

### 游戏端需要实现

| 接口 | 方法 | 说明 |
|------|------|------|
| UI 管理器 | `game.ui_manager` | 管理 UI 元素 |
| 元素注册 | `ui_manager.register(element)` | 注册 UI 元素 |
| 元素查找 | `ui_manager.get_element(id)` | 通过 ID 查找 |
| 元素查找 | `ui_manager.find_element_by_name(name)` | 通过名称查找 |
| 点击执行 | `ui_manager.click_at(x, y)` | 执行点击 |

### UI 元素需要实现

| 属性/方法 | 说明 |
|-----------|------|
| `element_id` | 唯一 ID |
| `name` | 可读名称 |
| `x, y, width, height` | 位置和大小 |
| `visible` | 是否可见 |
| `enabled` | 是否可交互 |
| `click()` | 点击事件处理 |
| `to_dict()` | 序列化 |

---

## 五、快速对接示例

```python
# 你的游戏代码

from engine.ui.manager import UIManager
from engine.ui.widgets import Button, InventoryPanel

class MyGame:
    def __init__(self):
        # 创建 UI 管理器
        self.ui_manager = UIManager()
        
        # 创建 UI 元素
        self._create_ui()
    
    def _create_ui(self):
        # 创建背包面板
        self.inventory = InventoryPanel()
        self.ui_manager.register(self.inventory)
        
        # 创建按钮
        self.btn_start = Button("btn_start", "开始游戏", self._on_start)
        self.btn_start.x = 400
        self.btn_start.y = 300
        self.ui_manager.register(self.btn_start)
    
    def _on_start(self):
        print("游戏开始！")
    
    def use_item(self, item_id):
        print(f"使用道具: {item_id}")
```

这样测试就可以通过 `game_client.ui_click(name="开始游戏")` 或 `game_client.item_use(slot_index=0)` 来测试了！
user: System: [2026-02-27 17:16:40 GMT+8] Feishu[opengl] DM from ou_15ff1d12b1e893ad7ae94f8124798591: 相关内容和文档都更新并提交下

Conversation info (untrusted metadata):
```json
{
  "message_id": "om_x100b552241b158b4b20f03807e42381",
  "sender": "ou_15ff1d12b1e893ad7ae94f8124798591"
}
```

相关内容和文档都更新并提交下
user: System: [2026-02-27 17:16:40 GMT+8] Feishu[opengl] DM from ou_15ff1d12b1e893ad7ae94f8124798591: 相关内容和文档都更新并提交下

Conversation info (untrusted metadata):
```json
{
  "message_id": "om_x100b552241b158b4b20f03807e42381",
  "sender": "ou_15ff1d12b1e893ad7ae94f8124798591"
}
```

相关内容和文档都更新并提交下
assistant: 🔺 已更新并提交！

## 新增内容

### 游戏端 UI 模块
```
src/engine/ui/
├── __init__.py      # 模块导出
├── element.py       # UI 元素基类
├── manager.py       # UI 管理器
└── widgets.py       # 组件（Button, ItemSlot, InventoryPanel 等）
```

### 测试端
```
tests/e2e/
├── client.py        # 新增 UI/道具方法
└── test_ui.py       # UI 交互测试示例
```

---

## UI 交互对接方式

### 游戏端实现

```python
from engine.ui import UIManager, Button, InventoryPanel

class MyGame:
    def __init__(self):
        # 1. 创建 UI 管理器
        self.ui_manager = UIManager()
        
        # 2. 注册 UI 元素
        self.inventory = InventoryPanel()
        self.ui_manager.register(self.inventory)
        
        self.btn = Button("btn_ok", "确认", self._on_click)
        self.ui_manager.register(self.btn)
```

### 测试端调用

```python
# 点击道具图标
client.ui_click(path="inventory_panel/item_slot_0")

# 通过名称点击
client.ui_click(name="确认")

# 点击坐标
client.ui_click_at(x=400, y=300)

# 使用道具
client.item_use(slot_index=0)
```

---

## 提交记录

```
e54ad3d feat: add UI interaction testing support
```

仓库：https://github.com/kongshan001/opencode-demo
